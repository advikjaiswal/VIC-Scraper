{
  "name": "rfp-intelligence-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Private RFP intelligence and proposal automation system for impact evaluation and development-sector opportunities.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/*.js && node --check scripts/*.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
